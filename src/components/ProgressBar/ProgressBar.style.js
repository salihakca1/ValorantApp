import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  progressBar: {
    height: 8,
    borderRadius: 4,
    backgroundColor: '#ddd',
    marginBottom: 5,
  },
  progress: {
    height: '100%',
    backgroundColor: 'red', 
    borderRadius: 5,
  },
});